<%= form_with(model: category, local: true, url: menu_categories_path(category.menu)) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-6">
    <%= form.label "Category Name", class:"label" %>
    <%= form.text_field :name, class:"input" %>
  </div>

  <div class="mb-6">
    <%= link_to "Add new category", "#", class:"link" %>
  </div>

  <div class="mb-6">
    <%= form.submit "Create Categories", class:"btn btn-sm btn-purple" %>
  </div>
<% end %>
